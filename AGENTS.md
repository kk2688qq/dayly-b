# 全球 AI 成功应用案例库维护规则

## 唯一目标

每天研究并新增一个全球范围内已经获得真实用户或商业验证的 AI 应用案例，帮助读者理解：创始人为什么开始、第一版如何验证、产品如何增长、怎样收费，以及哪些方法可以在中国低成本复刻。

网站不是 AI 新闻站、融资榜或产品目录。融资、媒体热度和大公司背景不能单独证明成功。

## 每次任务必读

1. `AGENTS.md`
2. `docs/DAILY_CASE_PROMPT.md`
3. `docs/CASE_METHODOLOGY.md`
4. `docs/FOUNDER_PROFILE.md`
5. `docs/TAXONOMY.md`
6. `templates/case-template.json`
7. `data/cases.json`
8. `assets/app.js`、`assets/case.js`
9. `package.json` 与 `scripts/validate.mjs`

## 两类任务必须分开

### A. 每日新增任务

- 必须新增一个未收录项目。
- 案例总数必须比修改前增加 1。
- 新编号必须为现有最大 P 编号加 1。例如最大编号为 P023，新案例必须为 P024。
- `publishedAt` 必须是执行当天，`status` 必须为 `published`。
- 必须更新根级 `updatedAt`。
- 更新旧案例不能替代当天新增。

### B. 历史案例维护

- 可以纠错、补来源、补创始人故事或更新产品现状。
- 保留原 `id` 和 `publishedAt`，写入案例级 `updatedAt`。
- 不计入“每日新增一个案例”。

## 全球成功案例入选标准

候选项目至少满足以下三项中的两项，并能找到公开证据：

1. 有持续使用的真实用户或明确用户群。
2. 有可验证的付费、收入、续费、规模或商业化信号。
3. 产品持续运营，并形成清晰的增长或留存机制。

优先选择独立创业者、小团队和可被普通创业者理解的应用。排除纯概念、一次性演示、只有融资没有产品验证、无法找到可靠来源的项目。

案例的最终目的不是推荐照搬，而是服务 `docs/FOUNDER_PROFILE.md` 中的个人创业者画像。方法论和中国 MVP 必须评估纯数字交付、AI 核心性、边际成本、自动化、付费、自传播、验证速度和个人能力匹配。

## 研究与写作底线

- 至少 3 个可靠来源，其中至少 1 个是一手来源。
- 事实、公司/创始人口径和编辑分析必须明确区分。
- 不编造收入、用户数、估值、CAC、LTV、毛利、转化率和增长率。
- 商业数字必须标明时间点、币种和口径；无法核验时写“未公开”。
- 必须包含完整创始人创业故事，不得只写产品介绍。
- 使用中文短句，先给结论，再解释原因；照顾非技术读者。

## 修改边界

- 日常案例任务只修改数据、规则、模板或验证脚本。
- 不修改 HTML、CSS 和视觉样式，除非用户明确要求。
- 不删除历史案例，不改写无关案例。
- 不把密钥、验证码或个人隐私写进仓库。

## 完成定义

只有同时满足以下条件才能报告完成：

1. 编号、slug、项目名称均不重复。
2. 完整案例按模板写入 `data/cases.json`。
3. `npm test` 通过。
4. Git commit 已进入 `main`。
5. GitHub Pages 工作流结论为 `success`。
6. 线上 `data/cases.json` 已出现新案例。
7. 实际打开线上案例页，标题、摘要、创始人故事和来源均能显示。
8. 首页案例数增加 1，“最近发布”指向当天新案例。
